export interface Article {
    id: string
    title: string
    guest: string
    original_link: string
    publish_date: string
    source_platform: string
    tags: string[]
    summary: string
    quotes: string[]
    cover_image: string
}
