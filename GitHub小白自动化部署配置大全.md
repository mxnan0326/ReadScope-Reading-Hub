# 🚀 零基础实现“飞书一键发布，自建网站秒级更新”全图文教程

在这个教程里，你不需要懂服务器怎么买，也不需要去学习复杂的后端语言。只要根据下面的傻瓜式填空，你就能让你的个人网站像顶级大厂的产品一样——**“编辑完文章，一按发布，网页几秒钟后自动变化”**！

## 为什么需要这个？（原理解释）

以往，如果你修改了网站的数据或者代码，你需要经过三步折磨：

1. 自己在电脑上敲一行黑窗（命令行）脚本。
2. 看着脚本跑完。
3. 把所有的东西像打包包裹一样，扔回 GitHub 或者重新部署。

而**有了 GitHub Actions + 飞书 Webhook 之后**：
我们专门聘请了一位远在美国的“免费云端打工人（GitHub Actions 机器人）”，并通过飞书建立了一条“跨洋电话专线”。

- **只要你在飞书改了状态** -> 飞书自动给机器人打个电话（发发消息）。
- **机器人收到指令** -> 马上在云端自己跑脚本，自己下载文章，自己把它扔到你的线上版本里！
这一切零成本、全自动。

---

## 🔧 三大核心任务分解（跟着做！）

### 任务一：在你的代码里给“打工人”写说明书

我们要写一张字条，告诉云电脑机器人：“只要接到了来自飞书的特殊召唤暗号（比如叫 `feishu_update`），你就开始执行拉取操作”。

1. 在你的项目代码文件夹里，按路径依次新建并打开文件：`.github/workflows/auto-update.yml`。
2. 复制并粘贴以下**所有代码**进去（直接照抄，连空格缩进都不要漏）：

```yaml
name: 自动更新飞书数据

# 设定触发机器人的条件：
on:
  # 1. 允许别人通过发特定的 API Webhook 暗号唤醒我（我们要用的就是这个！）
  repository_dispatch:
    types: [feishu_update]
    
  # 2. 定时每天早晚各自动跑一次兜底（UTC 时间下的 cron）
  schedule:
    - cron: '0 0,12 * * *'
    
  # 3. 允许你自己随时在 GitHub 网页上点个按钮强制唤醒
  workflow_dispatch:

# 设定给机器人分配的具体任务步骤：
jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出当前代码
        uses: actions/checkout@v4
        
      - name: 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: 给这台免费的电脑安装必要的库 (requests)
        run: |
          python -m pip install --upgrade pip
          pip install requests
          
      - name: 开始运行我们的拉取数据的脚本
        run: |
          python scripts/fetch_feishu.py
          
      - name: 最重要的一步：把抓下来的新文件自动点保存 (Commit)
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🤖 自动化: 从飞书拉取最新已发布的记录"
          branch: main
```

1. 写好之后，**把你的整个项目提交（Commit）并拉取（Push）同步到你的 GitHub 仓库上**。（如果没传上去了就不用继续往下看了，必须传上去才会在云端生效哦！）

---

### 任务二：去 GitHub 拿钥匙 (生成 Token)

为了保护隐私，别人是不能随便从外部唤醒你的打工机器人的。所以你需要专门为“飞书老妈”配一把专属的家门钥匙（Token）。

1. 登录打开 [GitHub（你的仓库大本营）](https://github.com/)
2. 点击右上角你的**头像**，然后选择下拉菜单底部的 **`Settings` (设置)**。
3. 看着左侧长长的菜单栏，**滑到最最下面**，点击最后的 **`Developer settings` (开发者设置)**。
4. 左边又出现菜单了，点击 **`Personal access tokens`** ，展开后点击 **`Tokens (classic)`**。
5. 看向右上角，点击 **`Generate new token` -> `Generate new token (classic)`** （这里可能让你输一下登录密码确认身份）。
6. **像填表一样设置这把钥匙的属性**：
   - **Note**（备注名）：填 `飞书自动唤醒专用` (随便取)。
   - **Expiration**（过期时间）：推荐点开菜单选 `No expiration` (永不过期)，免得这把锁以后生锈打不开。
   - **重点来了 ⚠️**：往下滑有一堆方框，**只需找到第一个最大的选项框叫 `repo`，并在它前面的小盒子里打上勾**。这表示这把配置给飞书老妈的钥匙，有全权操作这个项目仓库的权利。
7. 滑到页面最最底部，点击绿色的 **`Generate token`**。
8. **屏幕上会显示一大串长得像 `ghp_....` 开头乱码的字符：恭喜你，拿到绝版钥匙了。** 把它复制下来，存在电脑的任何一个记事本里（因为你一旦离开这个页面，出于安全，GitHub 就再也不会给你看它了）。

*另外，为了确保云机器人干完活有修改文章的权限，请点进你的项目仓库主页 -> 顶部菜单的 `Settings` -> 左侧菜单 `Actions` 内的 `General` -> 滑到最下面，将 **Workflow permissions** 选为 **Read and write permissions** 并点击 `Save`保存。这一步极其重要，否则绿灯亮不了。*

---

### 任务三：在飞书拉好专线（发送 Webhook）

现在我们让主角“飞书”带上这把钥匙。

1. 打开飞书多维表格。
2. 点击右上角的 **“自动化”** （这个长得像个小闪电的图标），选择 **“创建自定义流程”**。
3. 左边的 **触发器** 选择：`记录发生符合条件的变更时`。
   - 【监控字段】选：`状态`
   - 【触发条件】为：`等于` -> `已发布`
   - *（如果是别的字段你也可以依样画葫芦修改）*
4. 右边的 **执行动作** 选择向下滑动，找到 **“发送 HTTP 请求”**（注意：有的旧版飞书叫“发送 Webhook”，如果是新版请务必找带“HTTP”字眼的）。
   - 【请求类型 / Method】：选择下划列表里的 **`POST`**
   - 【网址】：仔细填写 `https://api.github.com/repos/你的Github用户名/你的仓库名称/dispatches`。 *(例如：`https://api.github.com/repos/mxnan0326/ReadScope-Reading-Hub/dispatches`)*
5. 点击**请求头 (Headers)**旁边添加两个字段设置（就像暗号前还要对口令）：
   - 第一行：左边键填 `Accept`，右边值填 `application/vnd.github+json`
   - 第二行：左边键填 `Authorization`，右边值填：`Bearer 【这里粘贴你在任务二里生成的那串带 ghp_ 的长乱码钥匙】`。（**千万注意：`Bearer` 字母后面必须跟着一个英文的空格！**）
6. **主体 (Body)** 内容类型选择 `JSON`。在输入框里写入任务一里面我们在代码里写下的那个最特殊的暗号：

   ```json
   {
     "event_type": "feishu_update"
   }
   ```

7. 给这条流程随便起个名字，最后点击右下角的**“保存”或者“启用”**。全宇宙的流程就打通啦！

---

## 🎉 验收时刻（见证奇迹的发生）

想看看你牛逼轰轰的自动化发布系统起没起作用？

1. 在飞书的表单里，找一个已发布的文章。把它的状态改成“草稿”，停顿一秒后，再改回**已发布**！
2. 立马转头切进你在 GitHub 的这个项目主页网页，点击上方中间的一排叫 **`Actions`** 的标签页。
3. 哇哦！你会看到列表最上方突然蹦出了一条新的正在旋转发黄的任务（名字就叫“🤖 自动化: 从飞书拉取...”）。
4. 点进去，等那一排排看不懂的代码跳跃。不出 1 分钟，它如果呈现打勾的**绿色盾牌**。
5. 这说明文章的新图片和 JSON 已经成功被下载并写入了你的最新代码中了！！你网站部署相关的那些程序都会立刻获得一份新文件，并实现实时翻天覆地的自动变样！
